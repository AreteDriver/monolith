dbs:
  - path: /data/monolith.db
    replicas:
      - type: s3
        bucket: $LITESTREAM_REPLICA_BUCKET
        path: monolith
        endpoint: $LITESTREAM_REPLICA_ENDPOINT
        region: auto
        access-key-id: $LITESTREAM_ACCESS_KEY_ID
        secret-access-key: $LITESTREAM_SECRET_ACCESS_KEY
        retention: 168h
        sync-interval: 10s
